# App Fonts

- UI and body text: `Inter`
- Display headings, including the guide page title: `DM Serif Display`

When changing copy, preserve the existing CSS font styles unless a design change explicitly requires otherwise.
